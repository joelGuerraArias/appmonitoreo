# grabaciones — límite del proyecto

**Solo modificar archivos dentro de esta carpeta.**

| Correcto | Prohibido (otro proyecto) |
|----------|---------------------------|
| `grabaciones\appMonitoreo.py` | `radioAnalizer\radioAnalizer.py` |
| `grabaciones\appMonitoreo.py` | `radioAnalizer\VIDEOAnalizer3.py` |

Abrir en Cursor: **`grabaciones.code-workspace`** o la carpeta `grabaciones` sola.

Hermano ignorado en índice: `../radioAnalizer/` (`.cursorignore`).
